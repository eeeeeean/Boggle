Switched to branch.rb

Using enumerator#any? turns out to be a lot faster than regex searches on arrays.

Create branches.

The board itself can be a hash, the key is a position array: [0,1] and the value is a letter.
